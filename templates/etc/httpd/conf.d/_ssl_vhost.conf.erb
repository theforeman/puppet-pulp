# allow older yum clients to connect, see bz 647828
  SSLInsecureRenegotiation On

  KeepAlive On
  MaxKeepAliveRequests <%= scope['::pulp::max_keep_alive'] %>

<% if scope['::pulp::ssl_username'] -%>
  SSLUsername <%= scope['::pulp::ssl_username'] %>
<%- end -%>
